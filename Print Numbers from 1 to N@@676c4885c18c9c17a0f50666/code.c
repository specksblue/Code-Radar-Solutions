#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n); // Input number

    for (int i = 1; i <= n; i++) { // Loop from 1 to n (including n)
        printf("%d", i);

    }

    return 0;
}
